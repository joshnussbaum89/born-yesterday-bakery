---
export interface Props {
  title: string
}

const { title } = Astro.props
---

<h1>{title}</h1>

<style>
  h1 {
    z-index: 1;
    padding: 0 1rem;
    font-size: clamp(var(--size-lg), 10vw, var(--size-xxl));
    font-weight: 600;
    text-align: center;
  }
</style>
